alias wget="curl -C - -O"
alias g="git status"
alias reload="source ~/bin/dotfiles/bash/aliases"
alias ea="mate -w ~/bin/dotfiles/bash/aliases && reload"

function gg() {
	git add .
	git commit -v -a -m "$*"
}

function take() {
	mkdir $1
	cd $1
}